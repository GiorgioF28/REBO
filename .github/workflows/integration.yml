name: Integration (Postgres + Spring)

on:
  pull_request:
    branches: ["develop", "main"]

jobs:
  backend-integration:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend

    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_DB: rebo
          POSTGRES_USER: rebo
          POSTGRES_PASSWORD: rebo
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U rebo -d rebo"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: "temurin"
          java-version: "21"
          cache: "maven"

      - name: Run integration profile tests
        env:
          SPRING_PROFILES_ACTIVE: test
          SPRING_DATASOURCE_URL: jdbc:postgresql://localhost:5432/rebo
          SPRING_DATASOURCE_USERNAME: rebo
          SPRING_DATASOURCE_PASSWORD: rebo
        run: |
          chmod +x mvnw
          ./mvnw -B test
